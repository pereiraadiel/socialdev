const Loader = () => (
  <div className="fixed top-0 left-0 z-50 w-full h-full bg-black bg-opacity-50 flex items-center justify-center backdrop-blur-sm">
    <div className="loader ease-linear rounded-full border-4 border-t-4 border-transparent border-t-blue-500 h-16 w-16 animate-spin"></div>
  </div>
);

export default Loader;
